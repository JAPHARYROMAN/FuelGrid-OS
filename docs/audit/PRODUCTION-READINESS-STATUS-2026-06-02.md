# FuelGrid OS — Production-Readiness Status (reconciled 2026-06-02)

> Generated by an 8-agent read-only reconciliation sweep of `PRODUCTION-READINESS-ROADMAP.md` Waves 3–10 against current `main` (migrations through 0084), plus direct verification of Waves 1–2. The roadmap and `99-findings-register*` are STALE — this file is the authoritative current open-work list. Trust the code, not the old register.

## Headline

Of the ~58 roadmap PR items across Waves 1–10, **the large majority are DONE**. Waves 1, 2, 4, 5, 7, 8 are effectively complete. The only genuinely-open engineering work is **finishing the last ~15% of the decimal-string contract** and **a few pagination-contract / ops items**. Two items are **deliberately deferred** with in-code rationale (FORCE-RLS, perpetual avg-cost), and one is the **long-term httpOnly-cookie migration** (roadmap marks it "lands last").

## STILL OPEN / PARTIAL (actionable)

| ID | Status | What remains | Evidence |
|---|---|---|---|
| W3-INV-STRING | PARTIAL | `setOpeningBalanceRequest.Litres` / `OpeningInput.Litres` still `float64` | inventory_handlers.go:131; internal/inventory/repo.go (OpeningInput) |
| W6-SDK-MONEY | PARTIAL | `Delivery.volume_litres` DTO still emits float; readings `volume_litres`/`dip_*_litres` + `variance_litres`/`opening_volume` still `number` in SDK; some request-param types still `number` | packages/sdk/src/types.ts:163-166,174,211; delivery DTO |
| W6-REL-HANDLERS / W9-REL-SDKPAGE | PARTIAL | SDK `Paginated<T>` lacks `has_more`/`offset`/`limit`; ~4 list handlers not yet on `parsePage`; web list pages don't send pagination params | pagination.go (has_more in Go only); types.ts Paginated |
| W9-OBS-BIZ | PARTIAL (ops) | `SENTRY_AUTH_TOKEN` not wired into CI → frontend sourcemap upload is a no-op until the token is provisioned | next.config.ts maybeWithSentry; ci.yml |
| W10-ARCH-SPLIT | PARTIAL (low value) | server.go already split into 20+ `register*` funcs; only further granular splitting of an 817-line file remains | server.go:276; server_routes*.go |

## DEFERRED — deliberate, documented (no action recommended now)

| ID | Rationale |
|---|---|
| W2-FORCE-RLS | RLS enabled on every tenant table + non-owner `fuelgrid_app` pool is the prod default with boot fail-stop + CI cross-tenant proof. `FORCE` would subject the owner (migrations/seed/outbox/background jobs) to RLS, requiring per-tenant GUCs everywhere. Documented in 0074_rls_coverage. Pure defense-in-depth; not needed for the isolation guarantee. |
| W4-FIN-COSTING | Cumulative average-cost is implemented + documented; perpetual moving-average deferred per docs/costing-policy.md. |
| W10-FE-COOKIE | httpOnly-cookie session migration is the roadmap's explicit long-term/high-churn item (touches auth_handlers, SDK transport, CORS). Bearer-token + presence-cookie + nonce-CSP is the current secure baseline. |
| MyShift `/me/shift` litres/price | Intentional float boundary for the attendant endpoint, per in-code comment. |

## DONE (verified) — selected highlights

- **Wave 3:** INV-DTO, SECRETS (redaction + prod validation), RLS-ORPHAN (0074 drift detector), OUTBOX-IMMUT (0075), ENT25-DB (0076 product-alignment FK), MIGRATE-GUARD (MIGRATE_CONFIRM).
- **Wave 4:** MD-MASTER, MD-READINGS, MD-PROC (products/tanks/nozzles/readings/PO decimal strings), FIN-INVDISC (per-receipt scoping), FIN-TAX (18% split test), QA-PROP (big.Rat zero-drift fuzz), QA-CONC (advisory-lock race test under -race).
- **Wave 5:** MD-SHIFT (shift-close decimal, SQL numeric), REL-REPO (parsePage in 42 handlers), REL-RATELIMIT (per-tenant + inflight).
- **Wave 6:** REL-DEGRADE (loadshed + Redis fail-open), OBS-CORR (trace↔log correlation).
- **Wave 7:** SDK-401 (forced logout), FE-MW (route guard + nonce CSP), UI-COMP (decimal-string props), SDK-ZOD (runtime validation), OBS-FE (Sentry capture + boundaries).
- **Wave 8:** OBS-BOUNDARY (global + segment error.tsx), FE-MONEY (formatMoney/sumMoney), FE-PERM (permission-gate + usePermission), FE-MUTERR (global MutationCache toast), FE-PWA (manifest).
- **Wave 9:** QA-WEBUNIT, QA-RTL, OPENAPI (v0.9.0, 4948 lines), CONTRACT-CI (CONTRACT_STRICT route diff), COV-GATE (Go + TS floors), E2E (5 Playwright specs), CD (GHCR + migrate + /readyz gate), SCAN (Trivy + SBOM + SARIF), MT-BLAST (cross-tenant isolation test).

## Recommended next action

Finish the decimal-string contract (W3-INV-STRING + W6-SDK-MONEY residuals) so there are no `number`/`string` mismatches between backend emission and the SDK, and add the `has_more`/`offset`/`limit` fields to the SDK `Paginated<T>` type while in that file. The remaining open items are either ops-gated (Sentry token), low-value (further server.go splitting), or deliberate long-term (httpOnly cookie).
