/**
 * Tailwind v4 ships its own PostCSS plugin. No further config required;
 * the rest of the build is driven by @import directives in globals.css.
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
